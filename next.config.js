/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "example.com", 
      "werycfblayepqkmwwpjp.supabase.co"
    ], // Replace with the domains of your image URLs
  },
}

module.exports = nextConfig